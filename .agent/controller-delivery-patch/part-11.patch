utSchemas)} | ${valueList(record.actorResolvers)} | ${valueList(record.authorizationCalls)} | ${valueList(record.applicationOperations)} | ${valueList(record.outputKinds)} | ${escapeCell(record.cachePolicy ?? '-')} | ${valueList(record.tests)} |`,
+  );
+  return `${GENERATED_HEADER}\n\n# HTTP and UI delivery contracts\n\nInventory SHA-256: \`${hashInventory(inventory)}\`\n\n| Entrypoint | Input schema | Actor | Authorization | Application operation | Output | Cache | Tests |\n| --- | --- | --- | --- | --- | --- | --- | --- |\n${rows.join('\n') || '| - | - | - | - | - | - | - | - |'}\n`;
+}
+
+function securityStatus(inventory: DeliveryInventory): string {
+  const rows = inventory.issues.map((issue) =>
+    `| ${issue.severity} | \`${issue.code}\` | \`${escapeCell(issue.file)}\` | ${escapeCell(issue.message)} |`,
+  );
+  return `${GENERATED_HEADER}\n\n# Delivery security status\n\nInventory SHA-256: \`${hashInventory(inventory)}\`\n\n| Severity | Code | File | Message |\n| --- | --- | --- | --- |\n${rows.join('\n') || '| pass | - | - | No delivery issue detected. |'}\n`;
+}
+
+export function renderDeliveryDocumentation(inventory: DeliveryInventory): Readonly<Record<string, string>> {
+  return {
+    'delivery-map.md': deliveryMap(inventory),
+    'http-contracts.md': httpContracts(inventory),
+    'security-status.md': securityStatus(inventory),
+  };
+}
+
+async function readOptional(filePath: string): Promise<string | null> {
+  try {
+    return await readFile(filePath, 'utf8');
+  } catch (error) {
+    if ((error as NodeJS.ErrnoException).code === 'ENOENT') return null;
+    throw error;
+  }
+}
+
+export async function generateDeliveryDocumentation(root = process.cwd()): Promise<readonly string[]> {
+  const inventory = await buildDeliveryInventory(root);
+  const directory = path.join(root, 'docs/90-generated/delivery');
+  await mkdir(directory, { recursive: true });
+  const written: string[] = [];
+  for (const [name, content] of Object.entries(renderDeliveryDocumentation(inventory))) {
+    await writeFile(path.join(directory, name), content, 'utf8');
+    written.push(`docs/90-generated/delivery/${name}`);
+  }
+  return written;
+}
+
+export async function checkDeliveryDocumentation(root = process.cwd()): Promise<readonly DeliveryIssue[]> {
+  const inventory = await buildDeliveryInventory(root);
+  const expected = renderDeliveryDocumentation(inventory);
+  const issues: DeliveryIssue[] = [];
+  for (const [name, content] of Object.entries(expected)) {
+    const file = `docs/90-generated/delivery/${name}`;
+    const actual = await readOptional(path.join(root, file));
+    if (actual !== content) {
+      issues.push({
+        severity: 'error',
+        code: actual === null ? 'DELIVERY_DOC_MISSING' : 'DELIVERY_DOC_DRIFT',
+        file,
+        message: actual === null
+          ? 'A generált delivery dokumentum hiányzik.'
+          : 'A generált delivery dokumentum nincs szinkronban az entrypointokkal.',
+      });
+    }
+  }
+  return issues;
+}
+diff --git a/packages/forge/src/delivery/generator.ts b/packages/forge/src/delivery/generator.ts
+new file mode 100644
+index 0000000000000000000000000000000000000000..e120f063ec5c2e7ec5e4d8c9dc14bcfa313c42c4
+--- /dev/null
++++ b/packages/forge/src/delivery/generator.ts
+@@ -0,0 +1,385 @@
++import { access, mkdir, readFile, writeFile } from 'node:fs/promises';
++import path from 'node:path';
++
++import { DocumentationCommandError } from '../documentation/types';
++import type { DeliveryGenerationKind, DeliveryGenerationResult } from './types';
++
++export type DeliveryGenerationOptions = Readonly<{
++  root?: string;
++  dryRun?: boolean;
++  force?: boolean;
++}>;
++
++type Target = Readonly<{
++  module: string;
++  feature: string;
++  operation: string;
++  pascalOperation: string;
++  camelOperation: string;
++}>;
++
++type PlannedFile = Readonly<{ file: string; content: string }>;
++type OperationMode = 'query' | 'command' | 'both';
++
++const GENERATED_HEADER = '// Generated by Winzard Forge. Review and implement the fail-closed placeholders before enabling this slice.';
++
++function pascal(value: string): string {
++  return value
++    .split('-')
++    .map((part) => `${part.slice(0, 1).toUpperCase()}${part.slice(1)}`)
++    .join('');
++}
++
++function camel(value: string): string {
++  const result = pascal(value);
++  return `${result.slice(0, 1).toLowerCase()}${result.slice(1)}`;
++}
++
++function parseTarget(value: string): Target {
++  const normalized = value.trim().replace(/^\/+|\/+$/gu, '');
++  const parts = normalized.split('/');
++  if (parts.length !== 3 || parts.some((part) => !/^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$/u.test(part))) {
++    throw new DocumentationCommandError(
++      'DELIVERY_GENERATOR_TARGET_INVALID',
++      'A cél formátuma module/feature/operation legyen, kebab-case szegmensekkel.',
++      value || '<hiányzó cél>',
++    );
++  }
++  const [module = '', feature = '', operation = ''] = parts;
++  return {
++    module,
++    feature,
++    operation,
++    pascalOperation: pascal(operation),
++    camelOperation: camel(operation),
++  };
++}
++
++function portFile(target: Target, mode: OperationMode): PlannedFile {
++  const declarations = [
++    ...(mode === 'query' || mode === 'both'
++      ? [
++          `export type ${target.pascalOperation}QueryPortInput = Readonly<{\n  query?: string;\n}>;`,
++          `export interface ${target.pascalOperation}QueryPort {\n  query(input: ${target.pascalOperation}QueryPortInput): Promise<Readonly<Record<string, unknown>>>;\n}`,
++        ]
++      : []),
++    ...(mode === 'command' || mode === 'both'
++      ? [
++          `export type ${target.pascalOperation}CommandPortInput = Readonly<{\n  payload: string;\n}>;`,
++          `export interface ${target.pascalOperation}CommandPort {\n  execute(input: ${target.pascalOperation}CommandPortInput): Promise<Readonly<Record<string, unknown>>>;\n}`,
++        ]
++      : []),
++  ];
++  return {
++    file: `src/modules/${target.module}/${target.feature}/application/ports/${target.operation}.port.ts`,
++    content: `${GENERATED_HEADER}\n\n${declarations.join('\n\n')}\n`,
++  };
++}
++
++function infrastructureAdapterFile(target: Target, mode: OperationMode): PlannedFile {
++  const importedTypes = [
++    ...(mode === 'query' || mode === 'both'
++      ? [`${target.pascalOperation}QueryPort`, `${target.pascalOperation}QueryPortInput`]
++      : []),
++    ...(mode === 'command' || mode === 'both'
++      ? [`${target.pascalOperation}CommandPort`, `${target.pascalOperation}CommandPortInput`]
++      : []),
++  ];
++  const interfaces = [
++    ...(mode === 'query' || mode === 'both' ? [`${target.pascalOperation}QueryPort`] : []),
++    ...(mode === 'command' || mode === 'both' ? [`${target.pascalOperation}CommandPort`] : []),
++  ];
++  const methods = [
++    ...(mode === 'query' || mode === 'both'
++      ? [`  async query(_input: ${target.pascalOperation}QueryPortInput): Promise<Readonly<Record<string, unknown>>> {\n    throw new Error('Implement ${target.pascalOperation}Adapter.query in the infrastructure layer.');\n  }`]
++      : []),
++    ...(mode === 'command' || mode === 'both'
++      ? [`  async execute(_input: ${target.pascalOperation}CommandPortInput): Promise<Readonly<Record<string, unknown>>> {\n    throw new Error('Implement ${target.pascalOperation}Adapter.execute in the infrastructure layer.');\n  }`]
++      : []),
++  ];
++  return {
++    file: `src/modules/${target.module}/${target.feature}/infrastructure/${target.operation}.adapter.ts`,
++    content: `${GENERATED_HEADER}\n\nimport 'server-only';\n\nimport type {\n  ${importedTypes.join(',\n  ')},\n} from '../application/ports/${target.operation}.port';\n\nexport class ${target.pascalOperation}Adapter implements ${interfaces.join(', ')} {\n${methods.join('\n\n')}\n}\n`,
++  };
++}
++
++function queryOperationFile(target: Target): PlannedFile {
++  return {
++    file: `src/modules/${target.module}/${target.feature}/application/queries/${target.operation}.ts`,
++    content: `${GENERATED_HEADER}\n\nimport type {\n  ${target.pascalOperation}QueryPort,\n  ${target.pascalOperation}QueryPortInput,\n} from '../ports/${target.operation}.port';\n\nexport type ${target.pascalOperation}Input = ${target.pascalOperation}QueryPortInput;\n\nexport type ${target.pascalOperation}Result = Readonly<{\n  kind: 'success';\n  value: Readonly<Record<string, unknown>>;\n}>;\n\nexport class ${target.pascalOperation} {\n  constructor(private readonly port: ${target.pascalOperation}QueryPort) {}\n\n  async execute(input: ${target.pascalOperation}Input): Promise<${target.pascalOperation}Result> {\n    return { kind: 'success', value: await this.port.query(input) };\n  }\n}\n`,
++  };
++}
++
++function policyFile(target: Target): PlannedFile {
++  return {
++    file: `src/modules/${target.module}/${target.feature}/application/policies/${target.operation}.policy.ts`,
++    content: `${GENERATED_HEADER}\n\nexport type ${target.pascalOperation}Actor = Readonly<{\n  subject: string | null;\n  roles: readonly string[];\n}>;\n\nexport class ${target.pascalOperation}Policy {\n  canExecute(_actor: ${target.pascalOperation}Actor): boolean {\n    return false;\n  }\n}\n`,
++  };
++}
++
++function commandOperationFile(target: Target): PlannedFile {
++  return {
++    file: `src/modules/${target.module}/${target.feature}/application/commands/${target.operation}.ts`,
++    content: `${GENERATED_HEADER}\n\nimport type { ${target.pascalOperation}CommandPort } from '../ports/${target.operation}.port';\nimport type { ${target.pascalOperation}Actor } from '../policies/${target.operation}.policy';\nimport { ${target.pascalOperation}Policy } from '../policies/${target.operation}.policy';\n\nexport type ${target.pascalOperation}CommandInput = Readonly<{\n  actor: ${target.pascalOperation}Actor;\n  payload: string;\n}>;\n\nexport type ${target.pascalOperation}CommandResult =\n  | Readonly<{ kind: 'success'; value: Readonly<Record<string, unknown>> }>\n  | Readonly<{ kind: 'forbidden' }>;\n\nexport class ${target.pascalOperation}Command {\n  constructor(\n    private readonly port: ${target.pascalOperation}CommandPort,\n    private readonly policy: ${target.pascalOperation}Policy,\n  ) {}\n\n  async execute(input: ${target.pascalOperation}CommandInput): Promise<${target.pascalOperation}CommandResult> {\n    if (!this.policy.canExecute(input.actor)) return { kind: 'forbidden' };\n    return {\n      kind: 'success',\n      value: await this.port.execute({ payload: input.payload }),\n    };\n  }\n}\n`,
++  };
++}
++
++function schemaFile(target: Target): PlannedFile {
++  return {
++    file: `src/modules/${target.module}/${target.feature}/presentation/${target.operation}.schemas.ts`,
++    content: `${GENERATED_HEADER}\n\nimport { z } from 'zod';\n\nexport const ${target.camelOperation}QuerySchema = z.object({\n  query: z.preprocess(\n    (value) => Array.isArray(value) ? value.at(-1) : value,\n    z.string().trim().max(200).optional(),\n  ),\n}).strict();\n\nexport const ${target.camelOperation}PayloadSchema = z.object({\n  payload: z.string().trim().min(1).max(10_000),\n}).strict();\n`,
++  };
++}
++
++function presenterFile(target: Target): PlannedFile {
++  return {
++    file: `src/modules/${target.module}/${target.feature}/presentation/${target.operation}.presenter.ts`,
++    content: `${GENERATED_HEADER}\n\nexport type ${target.pascalOperation}Response = Readonly<{\n  ok: true;\n  value: Readonly<Record<string, unknown>>;\n}>;\n\nexport function to${target.pascalOperation}Response(\n  value: Readonly<Record<string, unknown>>,\n): ${target.pascalOperation}Response {\n  return { ok: true, value };\n}\n`,
++  };
++}
++
++function viewFile(target: Target): PlannedFile {
++  return {
++    file: `src/modules/${target.module}/${target.feature}/presentation/${target.operation}-view.tsx`,
++    content: `${GENERATED_HEADER}\n\ntype ${target.pascalOperation}ViewProps = Readonly<{\n  value: Readonly<Record<string, unknown>>;\n}>;\n\nexport function ${target.pascalOperation}View({ value }: ${target.