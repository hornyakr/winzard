name: Publish verified controller delivery commit

on:
  push:
    branches:
      - agent/controller-delivery-complete-20260718
    paths:
      - '.github/workflows/agent-controller-delivery-publish.yml'
  workflow_dispatch:

permissions:
  contents: write
  statuses: write
  pull-requests: write

concurrency:
  group: controller-delivery-apply-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Apply exact implementation patch when needed
        shell: bash
        run: |
          set -euo pipefail
          if [[ -f packages/forge/src/delivery/inventory.ts && -f docs/public_documentation/winzard-controller.md ]]; then
            echo 'Implementation is already present.'
            exit 0
          fi

          expected='573bdf29d3495446e8d9a0376e05cdda99a34b5eb727195f75e5139e8b9b3151'
          output=/tmp/controller-delivery.patch
          test -f .agent/controller-delivery-patch/part-00.b64
          base64 --decode .agent/controller-delivery-patch/part-00.b64 \
            | xz --decompress --stdout > "$output"
          echo "$expected  $output" | sha256sum --check
          git apply --check "$output"
          git apply "$output"

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.13.1

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run complete core verification
        run: pnpm verify:core

      - name: Publish one clean implementation commit
        id: publish
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .agent
          rm -f .github/workflows/agent-controller-delivery-apply.yml
          rm -f .github/workflows/agent-controller-delivery-finalize.yml
          rm -f .github/workflows/agent-controller-delivery-publish.yml

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A

          tree="$(git write-tree)"
          parent="$(git rev-parse origin/main)"
          commit="$(printf '%s\n' 'feat: implement controller and delivery platform' | git commit-tree "$tree" -p "$parent")"

          git push --force origin "$commit:${GITHUB_REF_NAME}"
          echo "sha=$commit" >> "$GITHUB_OUTPUT"

      - name: Record verification on the final commit
        env:
          GH_TOKEN: ${{ github.token }}
          FINAL_SHA: ${{ steps.publish.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail
          target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          gh api --method POST "repos/${GITHUB_REPOSITORY}/statuses/${FINAL_SHA}" \
            -f state='success' \
            -f context='controller-delivery/verify-core' \
            -f description='pnpm verify:core passed on the published tree' \
            -f target_url="$target_url"

          pr_number="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$GITHUB_REF_NAME" --base main --state open --json number --jq '.[0].number // empty')"
          if [[ -n "$pr_number" ]]; then
            gh pr comment "$pr_number" --repo "$GITHUB_REPOSITORY" --body "Published clean commit \`${FINAL_SHA}\`. Full \`pnpm verify:core\` passed before publication; status \`controller-delivery/verify-core\` is attached to the final SHA."
            gh pr ready "$pr_number" --repo "$GITHUB_REPOSITORY" || true
          fi
