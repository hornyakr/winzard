# Contract governance recipe

Installs explicit contract and provider definition factories, generated contract registry startup validation, and empty application definition files. The Forge `contract:*` commands generate and verify the canonical inventory after the recipe is applied.
