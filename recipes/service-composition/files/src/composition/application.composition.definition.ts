import { defineComposition } from '@/platform/composition/contract';

export const applicationComposition = defineComposition({
  schemaVersion: 1,
  id: 'application',
  capability: 'service-composition',
  roots: [],
  services: [],
});
